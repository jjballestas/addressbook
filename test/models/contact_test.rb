require 'test_helper'

class ContactTest < ActiveSupport::TestCase
  # test "the post requires name and lastname" do
  #   contact = Contact.new
  #   assert !contact.save, "The contact was saved without required fields"

  #   puts "--Errors--"
  #   contact.errors.full_messages.each { |msg| puts msg }
  # end
end
